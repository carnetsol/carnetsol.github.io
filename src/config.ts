/**
 * Réglages du site.
 *
 * ------------------------------------------------------------------
 * STATISTIQUES DE LECTURE — GoatCounter
 * ------------------------------------------------------------------
 * Marche à suivre, une fois pour toutes :
 *
 * 1. Créez un compte sur https://www.goatcounter.com/signup
 *    Choisissez un identifiant de site : il devient l'adresse
 *    <identifiant>.goatcounter.com. Prenez « carnetsol » s'il est libre.
 *    Gratuit pour un usage non commercial.
 *
 * 2. Recopiez cet identifiant ci-dessous, entre les guillemets.
 *    Tant que la chaîne est vide, RIEN n'est ajouté aux pages : ni script,
 *    ni requête, ni compteur. Le site fonctionne exactement comme avant.
 *
 * 3. Pour que le nombre de lectures s'affiche à côté de chaque notule,
 *    il faut l'autoriser dans GoatCounter :
 *      Settings -> « Allow adding visitor counts to your website »
 *    Sans cette case, le compteur reste muet — le script échoue en silence
 *    et n'affiche rien plutôt qu'un zéro trompeur.
 *
 * CE QUE VOUS DEVEZ SAVOIR AVANT DE PUBLIER LES CHIFFRES
 * -----------------------------------------------------
 * - Le compteur n'est demandé que sur les PAGES DE NOTULE, jamais dans le
 *   fil : cinquante notules affichées feraient cinquante requêtes.
 * - Les bloqueurs de publicité arrêtent GoatCounter comme les autres. Les
 *   chiffres affichés sont donc un plancher, pas une mesure exacte.
 * - Sur un fonds de vingt ans, la plupart des notules affichent des
 *   nombres modestes. C'est vrai de tous les blogs, mais c'est plus
 *   confortable en privé : METTRE_LES_CHIFFRES_EN_PUBLIC ci-dessous permet
 *   de mesurer sans afficher.
 */

/** Identifiant GoatCounter. Vide = aucune mesure, aucun script. */
export const GOATCOUNTER = 'carnetsol';

/** false = on mesure, mais on n'affiche pas le compteur aux lecteurs. */
export const METTRE_LES_CHIFFRES_EN_PUBLIC = true;

/** Adresse de contact, commune au pied de page et aux commentaires. */
export const COURRIEL = 'carnetsol1598@gmail.com';
